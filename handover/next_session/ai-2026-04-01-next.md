# Next Session Instructions (auto-generated)

This file was created by the AI agent after completing Steps 1--3.

Short-term goals:
1. Finalize JSON schema metadata fields and ensure `schema_io.save_det_result` populates `metadata.computation_time_sec`, `metadata.computed_at`, `metadata.sample_size`, and `metadata.random_seed` correctly.
2. Implement extremal search algorithms (Step 4): functions to find matrices achieving det_max/det_min efficiently for n<=5.
3. Extend exhaustive computation to n=4 (if resources permit) and sampling for n=5.

Notes:
- Tests currently pass (13 tests).
- Data files for n=1..3 saved under `data/det_results/`.
- Use command `python scripts/compute_and_save_det.py` to recompute results.

Decisions made:
- Stored extremal matrices in `extremal_matrices` field in distribution results.
- Script is saved at `scripts/compute_and_save_det.py` and writes to `data/det_results/`.

Recommended next steps:
- Add unit tests for extremal matrix detection and schema I/O metadata population.
- Consider compressing or excluding large generated JSON from repo if they grow large.
